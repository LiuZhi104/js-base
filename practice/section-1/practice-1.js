'use strict';

function collectSameElements(collectionA, collectionB) {
            if(collectionA.join()==collectionB.join()){
              document.write("这两个元素为相同元素")；
            }else{
              document.write("不满足为相同元素")；
            }


  return '实现练习要求，并改写该行代码。';
}
